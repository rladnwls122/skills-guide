---
title: 05. 실습 — book 앱 배포와 ALB 연결
description: "LBC 설치와 book 앱 manifest 5종 적용, TG healthy·ALB 규칙을 검증하는 실습."
sidebar:
  order: 3
---

> 문서 유형: tutorial

원리와 채점 관점은 [05 이론 — k8s 워크로드와 ALB 연동](/part-2/05-k8s-workloads-alb/theory/)에 있다.

## 목표

- [ ] LBC를 helm으로 addon 노드에 설치
- [ ] book 앱 5종 manifest(ns→configmap→deployment→service/pdb→TGB)를 순서대로 apply
- [ ] TG 타깃 healthy 확인 + ALB 403/규칙 채점 명령 셀프 검증
- [ ] Pod 강제 삭제로 self-healing과 graceful shutdown 관찰

선행 지식과 목표는 [모듈 개요](/part-2/05-k8s-workloads-alb/)에.

:::note[실전 참조 — 막히면 여는 곳]
- helm values 키: `helm show values eks/aws-load-balancer-controller --version 3.4.0`, 브라우저는 [ArtifactHub](https://artifacthub.io/packages/helm/aws/aws-load-balancer-controller)
- YAML·CRD 필드: `kubectl explain deployment.spec.template.spec` · `kubectl explain targetgroupbinding.spec` (CRD 정본은 [TargetGroupBinding 문서](https://kubernetes-sigs.github.io/aws-load-balancer-controller/latest/guide/targetgroupbinding/targetgroupbinding/))
- CLI 플래그·출력 필드: `aws elbv2 describe-rules help` · `aws elbv2 describe-target-health help`
- 찾는 법 자체가 막히면 [실전 문서 검색 가이드](/reference/search-guide/)
:::

## 0. 사전 준비

- 정답지 저장소가 아직 없다면 [Day 0 §4 저장소 클론](/start/#4-저장소-클론). 경로는 클론 루트 `~\skills-2026` 기준이다.
- [04 실습](/part-2/04-eksctl-cluster/lab/)의 클러스터가 떠 있고, 같은 문서의 `.env.ps1` 이 만들어져 있어야 한다. 이 실습이 쓰는 `$env:VPC_ID`·`$env:ECR`·`$env:APP_TG`·`$env:ALB_DNS` 는 전부 그 파일에서 온다.
- 컨테이너 이미지가 ECR 에 `stable` 태그로 올라가 있어야 한다. 아직이면 [03 실습의 4단계](/part-1/03-container-lambda-dynamodb/lab/)를 먼저 한다 — 기본 CloudShell 에서 docker build/push 다.

## 1. AWS Load Balancer Controller

```powershell
cd ~\skills-2026\set-02\task-1
. .\.env.ps1

helm repo add eks https://aws.github.io/eks-charts; helm repo update
helm upgrade --install aws-load-balancer-controller eks/aws-load-balancer-controller `
  --version 3.4.0 -n kube-system `
  --set clusterName=wskorea26-cluster `
  --set serviceAccount.create=false `
  --set serviceAccount.name=aws-load-balancer-controller `
  --set region=ap-northeast-2 --set vpcId="$env:VPC_ID" `
  --set nodeSelector.node-type=addon
```

<details class="diagram-note">
<summary>이 블록이 하는 일</summary>

LBC 를 helm 으로 설치한다. `serviceAccount.create=false` 가 핵심이다 — 서비스 계정은 eksctl 이 IRSA 와 함께 이미 만들었으므로 helm 이 다시 만들면 IAM 연결이 끊긴 계정으로 덮인다. `nodeSelector.node-type=addon` 은 컨트롤러를 addon 노드에 고정하는 채점 항목이고, `vpcId` 는 컨트롤러가 서브넷을 찾을 때 쓴다.

</details>

**검증:**

```powershell
kubectl -n kube-system get deploy aws-load-balancer-controller
kubectl -n kube-system get pod -l app.kubernetes.io/name=aws-load-balancer-controller -o wide
```

기대: `2/2 READY`, 두 Pod 모두 **addon 노드** 위 (NODE 열의 노드를 `kubectl get node -L node-type`로 확인).

## 2. book 앱 배포 (순서 중요)

치환에 쓸 두 변수부터 확인한다. 비어 있으면 아래 apply 가 자리표시자를 그대로 넣어 `ImagePullBackOff` 로 간다.

```powershell
$env:ECR ; $env:APP_TG
```

기대: ECR 리포지토리 URI 한 줄(`<계정ID>.dkr.ecr.ap-northeast-2.amazonaws.com/wskorea26-book-repo`)과 타깃그룹 ARN 한 줄. 빈 줄이 나오면 `set-02\task-1` 에서 `. .\.env.ps1` 을 다시 실행한다. `.env.ps1` 자체가 없으면 이름으로 직접 조회해도 된다.

```powershell
$env:ECR    = aws ecr describe-repositories --repository-names wskorea26-book-repo `
  --query 'repositories[0].repositoryUri' --output text
$env:APP_TG = aws elbv2 describe-target-groups --names wskorea26-book-tg `
  --query 'TargetGroups[0].TargetGroupArn' --output text
$env:ALB_DNS = aws elbv2 describe-load-balancers --names wskorea26-book-alb `
  --query 'LoadBalancers[0].DNSName' --output text
```

```powershell
cd k8s
kubectl apply -f 00-namespaces.yaml
kubectl apply -f app/configmap.yaml
(Get-Content app/deployment.yaml -Raw).Replace('<ECR_REPOSITORY_URL>', $env:ECR) | kubectl apply -f -
kubectl apply -f app/service.yaml -f app/pdb.yaml
(Get-Content app/targetgroupbinding.yaml -Raw).Replace('<APP_TARGET_GROUP_ARN>', $env:APP_TG) | kubectl apply -f -
```

<details class="diagram-note">
<summary>이 블록이 하는 일</summary>

manifest 5종을 의존 순서대로 적용한다. 네임스페이스 → ConfigMap → Deployment → Service·PDB → TargetGroupBinding 이다. 가운데 두 줄은 파일을 그대로 쓰지 않고 자리표시자를 환경 변수 값으로 바꿔 표준 입력으로 넘긴다(`kubectl apply -f -`) — ECR 주소와 타깃그룹 ARN 은 계정마다 다르기 때문이다.

</details>

:::caution[순서 이유]
ns가 먼저(리소스의 소속), configmap이 deployment보다 먼저(envFrom 참조), TGB는 service 이후(serviceRef).
:::

**검증:**

```powershell
kubectl -n wskorea26 get pod -o wide
```

기대: `wskorea26-book-deploy-...` 2개 `Running 1/1`, 서로 **다른 AZ의 app 노드**.

```powershell
aws elbv2 wait target-in-service --target-group-arn "$env:APP_TG"
aws elbv2 describe-target-health --target-group-arn "$env:APP_TG" `
  --query "TargetHealthDescriptions[].[Target.Id,TargetHealth.State]" --output text
```

기대: Pod IP 2개 모두 `healthy`.

## 3. ALB 규칙 셀프 채점 (mark 7-1 / 7-2)

```powershell
$ALB_ARN = aws elbv2 describe-load-balancers --names wskorea26-book-alb --query "LoadBalancers[0].LoadBalancerArn" --output text
$LISTENER_ARN = aws elbv2 describe-listeners --load-balancer-arn $ALB_ARN --query "Listeners[0].ListenerArn" --output text

aws elbv2 describe-rules --listener-arn $LISTENER_ARN --query "Rules[*].Conditions[*].HttpHeaderConfig.Values[]" --output text
curl.exe -o NUL -s -w "%{http_code}`n" "http://$env:ALB_DNS/book"
```

<details class="diagram-note">
<summary>이 블록이 하는 일</summary>

ALB 규칙과 응답을 확인한다. 리스너 ARN 을 얻으려면 ALB ARN 이 먼저 필요해 두 단계로 조회한다. `describe-rules` 는 규칙 조건의 헤더 값을 뽑아 CloudFront 커스텀 헤더가 걸려 있는지 보여 주고, 마지막 `curl` 은 헤더 없이 ALB 를 직접 불러 403 이 나오는지 본다.

</details>

기대 출력 (정확 일치):

```
wskorea26-cf
wskorea26-cf
403
```

헤더를 붙여 직접 Pod까지 뚫어보기 (CloudFront 흉내):

```powershell
curl.exe -s -X POST -H "X-Origin-Verify: wskorea26-cf" -H "Content-Type: application/json" `
  -d '{"client_id":"T0001","username":"lab","email":"l@l.com","concert_name":"LAB"}' `
  "http://$env:ALB_DNS/v1/book"
```

기대: `{"booking_id": "..."}` (경로가 `/v1/book`인 이유 — 앱은 `/v1/book`만 서빙, CloudFront Function 재작성은 06에서).

## 4. 무중단 동작 관찰

```powershell
kubectl -n wskorea26 delete pod -l app=wskorea26-book --wait=false
kubectl -n wskorea26 get pod -w      # Ctrl+C로 중단
```

관찰 포인트: Terminating 상태가 preStop 15초 동안 유지 → 새 Pod가 Ready 된 후 타깃 교체. `describe-target-health`를 반복 실행하면 draining → healthy 전환이 보인다.

## 5. :badge[함정]{variant=danger}

| 함정 | 증상 | 대응 |
|---|---|---|
| PowerShell `curl` = Invoke-WebRequest 별칭 | 옵션 에러 | 항상 **`curl.exe`** 명시 |
| `<ECR_REPOSITORY_URL>` / `<APP_TARGET_GROUP_ARN>` 미치환 | ImagePullBackOff / TGB 에러 | Replace 파이프 확인 |
| LBC nodeSelector 누락 | LBC Pod가 app 노드 → mark 5-4 감점 | `--set nodeSelector.node-type=addon` |
| SA를 helm이 새로 생성 | IRSA annotation 없는 SA → AccessDenied | `serviceAccount.create=false` (eksctl IRSA SA 재사용) |
| TG health check 경로 불일치 | 타깃 unhealthy | TG `/health` = 앱 헬스 경로 확인 |
| 규칙 추가 실험 후 미삭제 | mark 7-2 줄 수 초과 | 규칙은 2개로 원복 |

## 6. 통과 기준 (mark 대응)

- [ ] mark 5-4: wskorea26 파드 전부 app 노드, kube-system 파드(예외 제외) 전부 addon 노드
- [ ] mark 7-1: `wskorea26-book-alb internet-facing` / `80 HTTP`
- [ ] mark 7-2: `wskorea26-cf` 2줄 + 403
- [ ] TG 타깃 2개 healthy

## 7. 정리

:::caution[과금 주의]
06으로 이어가면 **전부 유지**한다. 여기서 끝내면 이어지는 명령을 그 순서대로 실행한다. DynamoDB 삭제방지를 먼저 해제해야 `terraform destroy` 가 막히지 않는다.
:::

```powershell
cd ~\skills-2026\set-02\task-1
helm -n kube-system uninstall aws-load-balancer-controller
eksctl delete cluster -f eksctl/cluster.rendered.yaml --disable-nodegroup-eviction
aws dynamodb update-table --table-name wskorea26-data-table --no-deletion-protection-enabled
cd terraform; terraform destroy -var "player_number=$env:NUM"
```

`$env:NUM` 은 `.env.ps1` 이 담고 있다. 없으면 `$env:NUM = "<본인 비번호>"` 를 먼저 실행한다 — 값이 다르면 destroy 가 다른 이름의 리소스를 지우려 한다.

막히면 [트러블슈팅](/reference/troubleshooting/), 그날을 끝냈으면 [잔존 리소스 점검](/reference/cleanup-check/)을 돌린다.

