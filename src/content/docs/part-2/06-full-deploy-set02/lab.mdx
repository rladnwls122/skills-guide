---
title: 06. 실습 — set-02 1과제 완주 런북 (배포 10단계)
description: "배포 10단계를 런북 없이 완주하고 CloudFront curl 시나리오와 mark.sh 로 검증하는 실습."
---

> 문서 유형: tutorial

## 목표

- [ ] 10단계를 런북 없이(막히면만 참조) 완주
- [ ] CloudFront 경유 curl 시나리오 전부 기대값 일치
- [ ] CloudShell VPC Environment에서 mark.sh 실행, **80% 이상** 득점
- [ ] 정리 절차까지 완료 (과금 0 확인)

작업 루트: `C:\Users\kryuk\practice\skills-2026\set-02\task-1` (이하 상대 경로)
제공자료(`book`, `index.html`, `main.jpeg`)는 `..\..\shared\provided\task-1\` — 수정 금지.

---

## ① Terraform + 변수 영속화

```powershell
$env:AWS_DEFAULT_REGION = "ap-northeast-2"
$env:NUM = "103"                                  # 본인 비번호로 교체 (버킷/Grafana 계정에 쓰임)
cd terraform
terraform init
terraform apply -var "player_number=$env:NUM"
```

이어서 원본 README 배포 순서 1)의 **`.env.ps1` 영속화 블록**을 그대로 실행한다
(ACCOUNT_ID, VPC_ID, PRIV_SUBNET_C/D, CLUSTER_EXTRA_SG_ID, NODE_SG_ID, EKS_KMS_ARN,
BOOK_APP/LBC/FLUENT_BIT_POLICY_ARN, ECR, APP_TG, GRAFANA_TG, BUCKET, ALB_DNS, GRAFANA_ALB_DNS, CF
→ `..\.env.ps1` 저장 후 `. ..\.env.ps1`).

**검증**: `terraform output` 에러 없음, `.env.ps1` 존재. 새 터미널에선 `. .\.env.ps1`만 재실행.

## ② 이미지 빌드 & push — **기본 CloudShell** (VPC CloudShell 불가 — 인터넷 없음)

콘솔 기본 CloudShell에서 Actions → Upload file로 `book`(shared\provided\task-1)과 `app\Dockerfile` 업로드 후:

```bash
export AWS_DEFAULT_REGION=ap-northeast-2
ECR=$(aws ecr describe-repositories --repository-names wskorea26-book-repo --query 'repositories[0].repositoryUri' --output text)
mkdir -p ~/build && mv ~/book ~/Dockerfile ~/build/ && cd ~/build
aws ecr get-login-password | docker login --username AWS --password-stdin "${ECR%/*}"
docker build -t "$ECR:stable" .
docker push "$ECR:stable"

# 스캔 대기 + Critical/High 0 확인 (mark 3-1)
aws ecr wait image-scan-complete --repository-name wskorea26-book-repo --image-id imageTag=stable || \
  { aws ecr start-image-scan --repository-name wskorea26-book-repo --image-id imageTag=stable; \
    aws ecr wait image-scan-complete --repository-name wskorea26-book-repo --image-id imageTag=stable; }
aws ecr describe-image-scan-findings --repository-name wskorea26-book-repo --image-id imageTag=stable \
  --query 'imageScanFindings.findingSeverityCounts'
```

**검증**: 출력에 `CRITICAL`/`HIGH` 키가 **없어야** 함. High가 나오면 Dockerfile에 `RUN apk upgrade --no-cache` 추가 후 재빌드/재푸시.

:::danger[빌드 위치]
VPC CloudShell은 인터넷이 없어 alpine을 pull하지 못한다. 이미지 빌드는 반드시 **기본 CloudShell**에서.
:::

## ③ eksctl 클러스터 (~20분, ②와 병행 가능)

04 lab의 렌더 절차 그대로: `${VAR}` 9종 `.Replace()` 치환 → **`Select-String '\$\{'` 누락 검사** → `eksctl create cluster -f cluster.rendered.yaml` → `aws eks update-kubeconfig --region ap-northeast-2 --name wskorea26-cluster`.

**검증**: `kubectl get nodes --show-labels | Select-String node-type` → addon 2 + app 2.

## ④ AWS Load Balancer Controller

05 lab의 helm 블록 그대로 (`--version 3.4.0`, `serviceAccount.create=false`, `nodeSelector.node-type=addon`).

**검증**: `kubectl -n kube-system get deploy aws-load-balancer-controller` → 2/2, addon 노드.

## ⑤ book 앱

```powershell
cd ..\k8s
kubectl apply -f 00-namespaces.yaml
kubectl apply -f app/configmap.yaml
(Get-Content app/deployment.yaml -Raw).Replace('<ECR_REPOSITORY_URL>', $env:ECR) | kubectl apply -f -
kubectl apply -f app/service.yaml -f app/pdb.yaml
(Get-Content app/targetgroupbinding.yaml -Raw).Replace('<APP_TARGET_GROUP_ARN>', $env:APP_TG) | kubectl apply -f -
aws elbv2 wait target-in-service --target-group-arn "$env:APP_TG"
```

**검증**: wait가 통과하면 타깃 healthy.

## ⑥ 모니터링 (kube-prometheus-stack + Grafana)

:::danger[따옴표 함정]
Grafana 비번 `$korea26!!` 는 PowerShell **작은따옴표**로만 쓴다. 큰따옴표면 `$korea26` 이 빈 변수로 확장돼 비번이 틀리고 mark 10 이 전멸한다.
:::

```powershell
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts; helm repo update

# Grafana 계정 치환 — 비번 '$korea26!!'은 반드시 작은따옴표($ 확장 방지)
(Get-Content monitoring/kube-prometheus-stack-values.yaml -Raw).
  Replace('<GRAFANA_ADMIN_USER>', "skills-$($env:NUM)-admin").
  Replace('<GRAFANA_ADMIN_PASSWORD>', '$korea26!!') | Set-Content "$env:TEMP\kps-values.yaml"

helm upgrade --install wskorea26-monitoring prometheus-community/kube-prometheus-stack `
  --version 87.5.1 -n monitoring -f "$env:TEMP\kps-values.yaml"

# 대시보드 provisioning — grafana_dashboard=1 라벨이 있어야 사이드카가 로드
kubectl -n monitoring create configmap wskorea26-dashboard `
  --from-file=dashboard.json=monitoring/dashboard.json
kubectl -n monitoring label configmap wskorea26-dashboard grafana_dashboard=1

# Grafana ALB 연결
(Get-Content monitoring/grafana-targetgroupbinding.yaml -Raw).Replace('<GRAFANA_TARGET_GROUP_ARN>', $env:GRAFANA_TG) | kubectl apply -f -
aws elbv2 wait target-in-service --target-group-arn "$env:GRAFANA_TG"
```

**검증**: 브라우저 `http://<GRAFANA_ALB_DNS>/d/wskorea26/wskorea26-monitoring` 로그인
`skills-<비번호>-admin / $korea26!!` → POD/RESTART/NETWORK 패널에 데이터 (mark 10-1~10-4).

## ⑦ Fluent Bit (전 노드 DaemonSet)

```powershell
kubectl apply -f monitoring/fluent-bit.yaml
kubectl -n monitoring get ds fluent-bit
```

**검증**: DESIRED = 노드 수(4). 구성 요점: tolerations `operator: Exists`(전 노드), hostPath `/var/log/containers`, **cri parser**, 로그 그룹 `/wskorea26/eks/pod-logs`(Terraform이 생성, auto_create_group false). CloudWatch Logs에 `pod-` 스트림 생성 확인.

## ⑧ 검증 시드 (CloudFront Deployed 대기 후)

```powershell
$CF_ID = aws cloudfront list-distributions `
  --query "DistributionList.Items[?Comment=='wskorea26-concert-cf'].Id | [0]" --output text
aws cloudfront wait distribution-deployed --id "$CF_ID"     # mark 8-1이 Deployed 검사

curl.exe -o NUL -s -w "%{http_code}`n" "https://$env:CF"                 # 200
curl.exe -o NUL -s -w "%{http_code}`n" "http://$env:CF/"                 # 301
curl.exe -o NUL -s -w "%{size_download}`n" "https://$env:CF/main.jpeg"   # 180926
curl.exe -o NUL -s -w "%{http_code}`n" "http://$env:ALB_DNS/book"        # 403

curl.exe -s -X POST -H "Content-Type: application/json" `
  -d '{"client_id":"T0001","username":"tester","email":"t@t.com","concert_name":"SEED_CON"}' `
  "https://$env:CF/book"                                                 # {"booking_id": "..."}
curl.exe -s "https://$env:CF/book?concert_name=SEED_CON"                 # 최신순 배열
curl.exe -o NUL -s -w "%{http_code}`n" "https://$env:CF/book"            # 400
```

:::caution
주석의 기대값과 하나라도 다르면 여기서 멈추고 원인 추적 (아래 함정 표). 다음 단계로 넘어가지 않는다.
:::

## ⑨ 채점 — CloudShell VPC Environment

콘솔에서 VPC Environment 생성: **Subnet `wskorea26-priv-subnet-d` + SG `wskorea26-vpc-environment-sg`** (채점 항목 0). 파일 업로드가 안 되므로 mark.sh는 **heredoc 붙여넣기**:

```bash
rm -rf ~/.aws && aws configure          # default region: ap-northeast-2
cat > ~/mark.sh <<'MARKEOF'
# ← 본 PC의 mark.sh 내용 전체를 그대로 붙여넣기
MARKEOF
chmod +x ~/mark.sh && ~/mark.sh
```

**통과 기준: 자동 채점 80% 이상** (30점 만점 기준 24점+; 10번 Monitoring 3.5점은 수동 — Grafana 접속·패널로 셀프 확인).

## ⑩ 정리 (순서 엄수)

:::danger[순서 엄수]
helm → eksctl → DynamoDB 삭제방지 해제 → `terraform destroy`. 순서를 바꾸면 destroy 가 중간에 막힌다.
:::

```powershell
helm -n monitoring uninstall wskorea26-monitoring
helm -n kube-system uninstall aws-load-balancer-controller
eksctl delete cluster -f eksctl/cluster.rendered.yaml --disable-nodegroup-eviction
aws dynamodb update-table --table-name wskorea26-data-table --no-deletion-protection-enabled
cd terraform; terraform destroy -var "player_number=$env:NUM"
```

**검증**: destroy 완료 + 콘솔에서 EKS/ALB/NAT/CloudFront 잔존 0.

---

## :badge[함정]{variant=danger} 모음

| 함정 | 증상/감점 | 대응 |
|---|---|---|
| VPC CloudShell에서 빌드 시도 | alpine pull 실패 (인터넷 없음) | 이미지 빌드는 **기본 CloudShell** |
| `$korea26!!`를 큰따옴표로 | `$korea26`이 빈 변수로 확장 → 비번 틀림 (mark 10 전멸) | PowerShell **작은따옴표** `'$korea26!!'` |
| PowerShell `curl` | Invoke-WebRequest 별칭 | **`curl.exe`** 명시 |
| CF Deployed 전 채점 | 8-1 실패, /book 시나리오 불안정 | `aws cloudfront wait distribution-deployed` |
| book 바이너리/제공자료 수정 | 실격급 — 수정 금지 자료 | CF Function으로 우회 (POST /book→/v1/book) |
| ALB 규칙 추가/삭제 | 7-2 `wskorea26-cf` 2줄 정확 일치 깨짐 | 규칙 정확히 2개 유지 |
| 커스텀 헤더 정리 | 8-4 (X-Origin-Verify / wskorea26-s3-access 2줄) | 중복돼 보여도 제거 금지 |
| dashboard configmap 라벨 누락 | Grafana에 대시보드 안 뜸 (10-1) | `grafana_dashboard=1` 라벨 |
| DDB 삭제방지 해제 없이 destroy | destroy 중간 실패 | ⑩ 순서대로 |
| 비번호 교체 누락 | 버킷/Grafana 계정 이름 불일치 | `$env:NUM` 확인 (tfvars 기본 103) |

## 통과 기준 (mark 항목 요약)

- [ ] 1-1/1-2 네트워크, 2-1/2-2 S3, 3-1 ECR(stable, Critical/High 0), 4-1 DynamoDB
- [ ] 5-1~5-4 EKS (버전/로그/KMS/서브넷/NG/파드 배치)
- [ ] 6-1 Lambda(python3.14, TABLE_NAME), 7-1/7-2 ALB(403 + 헤더 2줄)
- [ ] 8-1~8-5 CloudFront (Deployed/Origin 2개/정책/헤더 2개/정적 웹 180926 bytes)
- [ ] 9-1~9-3 앱 (POST booking_id / GET 배열 / 파라미터 없으면 400)
- [ ] 10-1~10-4 Grafana 수동 확인
- [ ] **합계 80%+**

## 마무리 훈련 (Day 7 반복)

1. theory.md의 정답 도식을 보지 않고 백지에 그린다 — 화살표마다 인증 방식(OAC/커스텀 헤더/SG/IRSA) 주석까지. 5분 컷.
2. 10단계를 순서만 백지에 쓴다 (각 단계 한 줄 이유 포함).
3. 함정 표를 가리고 8개 이상 복기.
