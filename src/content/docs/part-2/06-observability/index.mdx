---
title: 06. 관측성 — Fluent Bit + kube-prometheus-stack + Grafana (Day 6)
description: "Day 6 모듈 개요 — 로그 파이프라인, log_to_metrics, Grafana provisioning, 알람 6종의 학습 목표. 1과제 후보 세 편이 모두 요구하는 스택이다."
---

import { FileTree, LinkButton } from '@astrojs/starlight/components';

> 문서 유형: 개요

관측성은 별도 심화 주제가 아니라 **1과제 안에 들어 있다.** 후보 세 편(set-02·set-03·set-07)이 모두 Prometheus·Grafana·Fluent Bit 을 요구하고, 배점은 30점 중 3.5~5.2점이다.

set-02 만 도구 선택이 자유롭고 수동 채점이다. set-03·set-07 은 스택이 고정되고 실시간 채점이다.

그래서 이 모듈은 [1과제 완주](/part-2/07-full-deploy-set02/) 앞에 온다. 완주 런북의 "모니터링·로깅" 단계가 여기서 배운 것을 그대로 쓴다.

## 학습 목표

- [ ] 관측성 파이프라인 전체(앱 로그 → Fluent Bit → CloudWatch + 로그→메트릭 → Prometheus → Grafana/알람)를 백지에 그릴 수 있다
- [ ] Fluent Bit DaemonSet의 구성요소(hostPath, cri 파서, tolerations, cloudwatch_logs 출력)를 설명할 수 있다
- [ ] `log_to_metrics` 필터로 액세스 로그에서 requests/errors counter + duration histogram을 만들고 `:2021/metrics` → PodMonitor로 스크레이프시킬 수 있다 (set-03 패턴)
- [ ] Lua로 Go duration 문자열(µs/ms/s/1m30s)을 초 단위 float로 변환하는 이유와 방법을 안다
- [ ] Grafana datasource/dashboard provisioning(configmap + `grafana_dashboard=1` 라벨 → sidecar import)을 할 수 있다
- [ ] PrometheusRule 알람 6종을 작성하고, 발화 가능/불가 항목을 구분할 수 있다
- [ ] 채점 전 "No Data 패널 = 오답" 원칙에 따라 트래픽을 시드할 수 있다

## 소요 시간 / 일차

- **Day 6** (1일)
- 이론 3h + 실습 4h (모듈 04~05에서 만든 클러스터 위에서 실습)

## 과금 리소스 & destroy 방침

| 리소스 | 요금(서울) | 비고 |
|---|---|---|
| EKS Control Plane + 노드 | 약 $0.31/h | 모듈 04~05에서 유지 중인 클러스터 재사용 |
| Grafana LoadBalancer(NLB) | 약 $0.028/h | kps values의 `service.type: LoadBalancer`가 생성 |
| CloudWatch Logs | 수집량 과금 (실습 수준 무시 가능) | 로그 그룹은 남으면 삭제 |

:::caution[과금 주의]
다음 모듈(07 완주, Day 7~8)은 런북 1단계부터 클러스터를 다시 만든다 — 이 모듈이 끝나면
`helm uninstall` + `eksctl delete cluster` 로 정리한다. 클러스터 방치 ≈ **$7.4/일**.
:::

## 선행 지식

- ConfigMap으로 설정을 주입하는 두 방법(환경변수 / 볼륨 마운트) — Fluent Bit·Grafana 설정이 전부 이 방식 — [k8s-basics](/start/k8s-basics/)
- Helm `install`/`upgrade --install`과 values 오버라이드 — [k8s-basics](/start/k8s-basics/)
- 라벨 셀렉터 — sidecar가 `grafana_dashboard=1` 라벨로 대시보드를 찾는다 — [k8s-basics](/start/k8s-basics/)
- 파이프·`grep`·`jq`로 로그에서 필요한 줄만 뽑는 원라이너 — [shell-basics](/start/shell-basics/)

막히는 항목이 있으면 그 링크를 먼저 읽는다. 아니면 바로 다음 파일로 간다.

## 선행 모듈

- 모듈 04 eksctl 클러스터, 05 워크로드/ALB — 앱이 떠 있어야 액세스 로그가 나온다

## 참고 경로

전부 `C:\Users\kryuk\practice\` 아래.

<FileTree>

- skills-2026
  - set-03/task-1
    - k8s/logging
      - **fluent-bit.yaml** log_to_metrics + Lua 완본
    - k8s/monitoring/ kps values, PrometheusRule 6종, dashboard.json
  - set-07/task-1
    - k8s/logging
      - **fluent-bit.yaml** 5키 JSON 재구성 패턴
  - set-02/task-1
    - k8s/monitoring/ IRSA 세트의 변형
  - docs/src/content/docs/setlist/set-03/task-1
    - **notes.md** 함정 실측 기록

</FileTree>

## 공식 문서

- [Fluent Bit 매뉴얼](https://docs.fluentbit.io/manual) — INPUT/FILTER/OUTPUT 문법, `log_to_metrics`, Lua 필터
- [kube-prometheus-stack 차트](https://github.com/prometheus-community/helm-charts/tree/main/charts/kube-prometheus-stack) — values 키 전체 목록
- [PromQL 기초](https://prometheus.io/docs/prometheus/latest/querying/basics/) — 알람 표현식 작성
- [Grafana 프로비저닝](https://grafana.com/docs/grafana/latest/administration/provisioning/) — datasource·dashboard 파일 규격
- [CloudWatch Logs Insights 쿼리](https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/AnalyzingLogData.html) — 로그 검증용 쿼리 문법

## 다음 단계

<LinkButton href="/part-2/06-observability/theory/" icon="open-book">이론 읽기</LinkButton>
<LinkButton href="/part-2/06-observability/lab/" variant="minimal" icon="rocket">실습 시작</LinkButton>
