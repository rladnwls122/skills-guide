---
title: Lab 10 — 오토스케일링 + 로깅 + 스트리밍
description: 유형 3 SQS→KEDA→Karpenter와 유형 4 OTel→Loki→Grafana를 순서·검증·함정 중심으로 배포하고, 유형 5 Kinesis/Flink·MSK는 정답지 대조를 필수로, 실제 배포는 선택 경로로 다룬다.
sidebar:
  order: 3
---

import { Steps } from '@astrojs/starlight/components';

> 문서 유형: tutorial

정답지: `skills-2026/set-07/task-2/`. terraform은 본 PC(PowerShell), 클러스터/헬름/이미지 빌드는 CloudShell(Docker 내장)에서 한다 — 채점과 같은 셸이라 클러스터 생성자 신원 = 채점 신원이 자동 일치한다. 상세 명령 전문은 각 모듈 README 런북에 있다. 이 랩은 순서·검증·함정에 집중한다.

## 실습 목표

- [ ] (유형 3) SQS→KEDA→Karpenter 스택을 배포하고 메시지 100건 → Pod ≥ 5 / Node ≥ 2, purge → Pod 1 / Node 1 수렴을 재현한다
- [ ] (유형 4) OTel→Loki→Grafana 파이프라인을 배포하고 LogQL로 ERROR 로그를 조회, 대시보드 3패널을 채운다
- [ ] (유형 5) 정답지 코드를 mark.md 2-1~2-6·4-1~4-5-B와 전부 대조한다 — 실제 배포는 선택(Part C)
- [ ] 두 클러스터·VPC를 전부 정리한다

---

## Part A — 유형 3 · 오토스케일링 (ap-northeast-2, skm-)

### A-1. 절차 (순서 요약)

<Steps>

1. **본 PC**: `module-3-eks-scaling/terraform` apply → `outputs.json` → eksctl/k8s/mark/provided+outputs.json을 zip으로 CloudShell 반입.
2. **CloudShell**: eksctl·helm 설치 → `~/.skm-env` 환경변수 영구화 → `eksctl create cluster`(~20분, **탭 닫지 말 것**). 클러스터가 2개(모듈 3·4)가 되면 **모듈 전용 터미널에서 `KUBECONFIG`를 모듈 경로로 고정**하고 시작한다 — 터미널 1개 = 클러스터 1개. 공유 kubeconfig의 `use-context` 전환은 잊는 순간 엉뚱한 클러스터에 apply 된다.
3. 앱 이미지 빌드/푸시 (제공 Dockerfile 그대로).
4. **KEDA 2.20.1** 설치 — `serviceAccount.operator.create=false`(IRSA용 keda-operator 재사용) + addon taint `tolerations` 지정.
5. **Karpenter 1.14.0** 설치(kube-system) — `--set replicas=1`(차트 기본 2는 addon 노드 1대에서 `helm --wait` 무한 대기) + `controller.resources.requests.cpu=0.5, memory=512Mi`로 낮춤(addon 노드 용량).
6. k8s 번호 순서 apply: namespace → **NodePool 먼저** → deployment(`<ECR_IMAGE>`/`<SQS_URL>` sed 치환) → ScaledObject.

</Steps>

### A-2. 검증 + 기대 출력

```bash
kubectl get pods -n keda                                      # 3개 모두 Running (Pending이면 A-4 함정 1)
kubectl get pods -n kube-system -l app.kubernetes.io/name=karpenter   # 1개 Running
kubectl get nodes -l karpenter.sh/nodepool=skm-app-nodepool   # 1대
kubectl get pods -n skillsmkt -o wide                         # order-processor 1/1 Running
kubectl get scaledobject -n skillsmkt                         # order-scaler READY True

# 스케일아웃: 메시지 100건 주입 (mark3.sh가 수행) 후 관찰
kubectl get pods -n skillsmkt -w        # 1 → 5 (queueLength 5, max 5)
kubectl get nodes -l karpenter.sh/nodepool=skm-app-nodepool   # 2대 이상 (500m×5 > 1.9 vCPU)

# 스케일인: purge 후 최대 2.5분 관찰
kubectl get pods -n skillsmkt           # 1개로 수렴
kubectl get nodes -l karpenter.sh/nodepool=skm-app-nodepool   # 1대로 수렴
```

<details class="diagram-note">
<summary>이 블록이 하는 일</summary>

스케일 동작을 위에서 아래로 확인한다. 먼저 KEDA·Karpenter·워커·ScaledObject 가 정상인지 보고, 메시지 100건이 들어간 뒤 파드가 1에서 5로 늘고 노드가 따라 느는지 `-w` 로 지켜본다. 파드 5개의 요청량이 노드 한 대의 할당 가능 CPU 를 넘기 때문에 노드가 2대 이상이 되는 것이 정상이다. 큐를 비우면 반대로 수렴한다.

</details>

채점: `bash ~/module-3/mark/mark3.sh` — 3-6 Max Ready Pods 5 / Max App Nodes 2 이상, 3-7 Final Pods 1 / Final Nodes 1.

### A-3. :badge[함정]{variant=danger}

1. **KEDA Pod Pending** → 차트 tolerations 스키마 확인: `helm show values kedacore/keda --version 2.20.1 | grep -n -A3 tolerations`. 버전에 따라 키가 최상위/컴포넌트별로 바뀐다.
2. **addon 노드 용량 초과** → Karpenter requests 0.5/512Mi 축소 설치. 그래도 Pending이면 KEDA metricServer/webhooks requests도 축소.
3. **scaleDown stabilizationWindowSeconds(15)·consolidateAfter(60s)를 늘리면** 스케일인 채점(150초 창) 실패. ScaledObject에 pollingInterval은 두지 않는다 — min ≥ 1이면 스케일링이 HPA sync 주기를 따라 이 값이 감지 속도를 바꾸지 못한다.
4. **deployment env는 리터럴 3개만** — valueFrom/추가 금지 (name=value 정확 비교).
5. NodePool보다 deployment를 먼저 apply하면 앱 Pod가 스케줄될 노드가 없다 — 번호 순서 준수.
6. 디스커버리 태그 누락(서브넷 `karpenter.sh/discovery`, SG `aws:eks:cluster-name`) → 노드가 영원히 안 뜬다.
7. **Karpenter `helm --wait`가 안 끝나면 replicas부터** — 기본 2 + required podAntiAffinity라 addon 노드 1대에선 구조적으로 불가(`--set replicas=1`). 행 상태에서 중단했다면 release가 `pending-upgrade`로 잠긴다 → `helm uninstall karpenter -n kube-system` 후 재설치.

### A-4. 통과 기준

- [ ] order-scaler READY True, 초기 Pod 1 / 앱 노드 1
- [ ] 100건 주입: Pod ≥ 5, 앱 노드 ≥ 2
- [ ] purge: 2.5분 내 Pod 1 / 노드 1
- [ ] mark3.sh 전 항목 PASS

---

## Part B — 유형 4 · 로깅 파이프라인 (ap-northeast-1, o11y-)

:::caution[module-4 정답지는 미착수다]
`set-07/task-2/module-4/`는 디렉터리 구조만 있다(근거는 `task.md`·`mark.md` 파일의 모듈 4 절과 `mark/mark4.sh` 스크립트).

이 모듈의 절차는 그 근거로 짠 **설계안**이라 차트 버전·파일 경로는 구현 시 달라질 수 있다. 채점 항목과 순서 의존(LBC → TGB, Loki → OTel)을 익히는 용도로 읽는다. 정답지가 구현되면 그 README를 우선한다.
:::

### B-1. 절차 (순서 요약 — 순서 의존이 많다)

<Steps>

1. **본 PC**: `module-4/terraform` apply (VPC + **이름 고정 ALB/TG** + ECR + LBC IAM) → zip 반입.
2. **CloudShell**: `~/.o11y-env` → `eksctl create cluster`(~20분) → 노드 2대가 AZ a/c로 분산됐는지 확인.
3. 이미지 빌드: **제공 app.py + 자체 app/Dockerfile** (제공 Dockerfile은 flask 미설치 → CrashLoop).
4. **LBC 3.4.2 설치** ← TGB CRD가 여기서 생긴다. 이후에만 TGB apply 가능.
5. namespace/StorageClass/앱 apply + 앱 TGB.
6. **Loki 18.5.1** (`grafana-community` 저장소 — `grafana/loki`는 GEL 전용) → svc `o11y-loki` 3100 확인.
7. **OTel DaemonSet apply는 Loki 이후** (otlphttp 전송 대상 먼저).
8. **Grafana 10.5.15** + 대시보드 ConfigMap + Grafana TGB.

</Steps>

### B-2. 검증 + 기대 출력

```bash
curl -s "http://$APP_ALB/healthz"                     # {"status":"ok"}
curl -s "http://$APP_ALB/log?level=error&count=3"     # {"generated":3,"level":"error"}
kubectl get ds o11y-otel -n monitoring                # DESIRED 2 / READY 2
kubectl get svc o11y-loki -n monitoring               # ClusterIP 3100

# LogQL 직접 조회 (port-forward)
kubectl port-forward -n monitoring svc/o11y-loki 3100:3100 & sleep 2
curl -s -G http://localhost:3100/loki/api/v1/query_range \
  --data-urlencode 'query={k8s_namespace_name="o11y"} | json | level="ERROR"' \
  --data-urlencode "start=$(date -d '3 minutes ago' +%s)000000000" \
  --data-urlencode "end=$(date +%s)000000000" | jq -r '.data.result[].values[][1]' | head -3
# 기대: {"ts":...,"level":"ERROR",...} JSON 원문 라인들
kill %1
```

<details class="diagram-note">
<summary>이 블록이 하는 일</summary>

로그 파이프라인을 끝에서 끝까지 확인한다. 앱에 로그를 일부러 만들게 하고, 수집기 DaemonSet 이 노드 수만큼 떠 있는지, Loki 서비스가 있는지 본다. 마지막은 진짜 확인이다 — port-forward 로 Loki 에 직접 LogQL 을 던져 방금 만든 ERROR 로그가 JSON 원문 그대로 돌아오는지 본다. 본문이 훼손되면 `| json` 파싱이 깨져 여기서 빈 결과가 나온다.

</details>

채점 전 마지막 준비: `/log?level=info`, `/log?level=warn`, `/log?level=error` 각각 호출 → Grafana Log Overview 3패널 모두 데이터 확인(범례 INFO/WARN/ERROR). 채점: `bash ~/module-4/mark/mark4.sh` (4-6은 Grafana 웹 수동 채점).

### B-3. :badge[함정]{variant=danger}

1. **제공 Dockerfile로 빌드 → CrashLoop.** 자체 Dockerfile 사용, app.py 수정 금지.
2. **TGB를 LBC 설치 전에 apply → CRD 없음 오류.** 순서: LBC → TGB.
3. **ALB를 Ingress로 만들면 이름 채점 실패** — 이름 고정이 필요하므로 terraform ALB + TGB.
4. **level은 대문자 ERROR** — LogQL `| json | level="ERROR"`. 소문자로 찍히게 가공하면 오답.
5. **범례는 plain text**(`ERROR`)여야 한다 — `{level="ERROR"}` 형태로 뜨면 오답. `sum by (level)` + legendFormat `{{level}}`.
6. **No Data 패널 1개라도 있으면 오답** — 채점 전 3레벨 호출.
7. **로그 확인(4-5)은 부분 일치**로 완화됐다(배포본 v5) — "3분 이내로 기록된 로그가 있으면 득점"이라 본문을 맞출 필요 없이 **채점 직전 트래픽을 한 번 흘려 최근 로그를 만들어 두면** 된다. 모든 노드 TZ는 KST로 설정한다.
7. OTel이 body를 가공하면 `| json` 파싱 깨짐 — container 파서 외 본문 변형 금지.
8. mark4.sh의 `rm -rf ~/.aws`는 CloudShell(관리형 세션)에선 무해 — bastion에서 돌리지 말 것.

### B-4. 통과 기준

- [ ] /healthz ok, ds 2/2, svc 3100
- [ ] LogQL ERROR 조회에 원본 JSON 라인 반환
- [ ] Grafana 3패널 데이터 + 범례 INFO/WARN/ERROR
- [ ] mark4.sh 자동 항목 PASS

---

## Part C — 유형 5 · 스트리밍 (Kinesis + Flink Studio / MSK, 선택 배포)

정답지: `skills-2026/set-02/task-2/`(set-07이 아니다 — 유형 5는 다른 세트). 배포 없이 C-1만 마쳐도 학습 목표를 채울 수 있다. C-2(실제 배포)는 시간·과금 여유가 있을 때만 한다.

### C-1. 만들지 않고 대조하기 (필수)

<Steps>

1. `mark.md`의 "2) Real-time Data Analytics"·"4) MSK" 절(2-1~2-6, 4-1~4-5-B)을 읽는다 — 채점이 보는 값이 곧 학습 목표다.
2. `module-2-analytics/terraform/kinesis.tf`·`flink.tf`, `module-4-msk/terraform/msk.tf`·`security.tf`·`lambda.tf`를 정독하며 mark 항목과 한 줄씩 맞춰본다. 예:
   - mark 2-4 기대값 `wsc2026-analytics-flink READY ZEPPELIN-FLINK-3_0` ↔ `flink.tf`의 `var.flink_app_name`·`var.flink_runtime` 기본값이 정확히 이 값.
   - mark 4-3 기대값 `... Sasl.Iam.Enabled True` ↔ `msk.tf`의 `client_authentication.sasl.iam = true`(`unauthenticated`는 `producer_auth_mode`로 별도 제어 — mark는 이 값을 보지 않는다).
3. `module-2-analytics/README.md`·`module-4-msk/README.md`의 "요구사항 ↔ 구현 매핑" 표로 나머지 항목(2-1·2-2·2-5·2-6, 4-1·4-2·4-4·4-5)을 전부 훑는다.
4. (선택, 콘솔) Managed Apache Flink 콘솔에서 Studio 노트북 생성 마법사를 끝까지 진행해 본다(생성 직전 취소) — 위저드가 자동으로 붙이는 커넥터 체크박스를 확인하면 [이론](/part-4/10-scaling-logging-streaming/theory/)의 "커넥터를 Maven으로 명시 주입" 함정이 왜 필요한지 눈으로 이해된다.
5. (선택, 콘솔) MSK 콘솔에서 클러스터 생성 마법사의 "액세스 제어 방법" 화면까지 진행해 본다(생성 직전 취소) — IAM 역할 기반 인증과 미인증 액세스를 동시에 켜는 체크박스 위치를 확인한다.
6. `terraform plan`으로 실제 인자만 확인한다(리소스를 만들지 않으므로 과금 없음):

   ```powershell
   cd module-2-analytics\terraform ; terraform init ; terraform plan -var "player_number=$env:NUM"
   cd ..\..\module-4-msk\terraform ; terraform init ; terraform plan -var "player_number=$env:NUM"
   ```

</Steps>

### C-2. 실제로 만들 경우 — 최단 경로 + 즉시 destroy (선택)

module-2(Kinesis+Flink)는 생성 지연이 없어 부담이 적다. module-4(MSK)는 **생성 ~30분 + 시간당 과금**이 커서 시간 여유가 있을 때만 진행한다. 두 모듈은 독립적이라 하나만 골라도 된다.

<Steps>

1. **module-2**(본 PC): apply → EC2 user_data(pip 설치)·TG 헬스체크까지 2~3분 대기.
   ```powershell
   cd module-2-analytics\terraform
   terraform apply -var "player_number=$env:NUM"
   ```
2. Flink 콘솔 → Studio → `wsc2026-analytics-flink` → **Run** → Zeppelin 열기 → `%flink.conf parallelism.default 1`을 **첫 문단**으로 실행 → 베이스 테이블/뷰 DDL → 과제지 두 SQL 쿼리 실행 확인 → **반드시 Stop**(RUNNING이면 mark 2-4 오답).
3. 확인 즉시:
   ```powershell
   terraform destroy -var "player_number=$env:NUM" -auto-approve
   ```
4. **module-4**(본 PC, 시간 여유가 있을 때): 의존성 번들 → apply(**~30분**) → producer 부팅·ESM 안정화 3~5분 대기.
   ```powershell
   cd module-4-msk\terraform
   py -m pip install -r lambda\sensor_consumer\requirements.txt -t lambda\sensor_consumer\
   terraform apply -var "player_number=$env:NUM"
   ```
5. 확인 즉시:
   ```powershell
   terraform destroy -var "player_number=$env:NUM" -auto-approve
   ```

</Steps>

```bash
# 채점 스크립트로 한 번에 확인 (set-02/task-2 에서, module-4는 비번호를 물어본다)
bash mark/mark2-2.sh
bash mark/mark2-4.sh
```

Part C를 배포했다면 destroy까지 여기서 끝낸다 — 마지막 "정리" 절의 EKS 목록에는 module-2·module-4가 들어 있지 않다.

### C-3. :badge[함정]{variant=danger}

1. **Flink Studio는 `aws_kinesisanalyticsv2_application`으로 못 만든다** — zeppelin 설정 블록 미지원(provider issue #41233). `aws_cloudformation_stack`으로 래핑.
2. **Kinesis SQL 커넥터는 bare CFN에 없다** — `CustomArtifactsConfiguration`에 Maven `flink-sql-connector-kinesis:1.15.4` 명시 주입. 안 하면 `Could not find any factory for identifier 'kinesis'`.
3. **bare 타임스탬프**(`2026-07-13 08:10:51`, T/Z 없음) — 베이스 테이블 `TIMESTAMP(3)` 파싱 + 뷰에서 `TIMESTAMP_LTZ(3)` CAST로 우회. 자세한 이유는 [이론](/part-4/10-scaling-logging-streaming/theory/).
4. **`%flink.conf parallelism.default 1`은 인터프리터 기동 전, 첫 문단에서만** 먹는다. 세션이 망가지면 인터프리터 재시작.
5. **채점 직전 Flink 상태는 READY** — mark 2-4는 `wsc2026-analytics-flink READY ZEPPELIN-FLINK-3_0`을 본다. RUNNING이면 오답.
6. **제공 producer 바이너리는 SASL/IAM 서명 구현이 없다**(리버싱으로 확인) — 9098을 주면 `unexpected EOF: broker appears to be expecting TLS`. 기본값(`producer_auth_mode=tls`)이 실측 정답 경로다.
7. **DynamoDB는 전 속성 String 저장** — mark 4-5-A가 `temperature.S`/`status.S`로 조회한다. Number로 저장하면 값이 안 잡혀 0점.
8. **ESM은 `starting_position = LATEST`** — TRIM_HORIZON으로 두면 재배포 시 백로그 재처리가 폭주한다.
9. **env 이름 한 글자 차이**: producer `BOOTSTRAP_SERVERS`(복수) / consumer `BOOTSTRAP_SERVER`(단수). 통일하면 둘 중 하나가 깨진다.
10. **4-5-B는 `--output json`**으로 `SENSOR-002`의 `sensorId`·`timestamp`만 조회하고 `timestamp`가 ISO 8601 KST여야 한다(정정 전 `--output table`·`max-items 3`에서 변경 — [정정 내역](/reference/errata/)). 4-1의 S3 버킷 ARN은 비번호를 제외하고 채점한다.

### C-4. 통과 기준

- [ ] mark.md 2-1~2-6, 4-1~4-5-B 값을 정답지 코드와 전부 대조했다
- [ ] (배포했다면) mark2-2.sh·mark2-4.sh 전 항목 PASS
- [ ] (배포했다면) Flink는 Stop 후 READY 확인, MSK 관련 리소스는 destroy까지 완료

---

## 정리 (당일 필수 — EKS 2개가 최대 과금원)

```bash
# CloudShell (각 리전에서)
kubectl delete -f k8s/ --ignore-not-found        # TGB·NodePool 먼저 (Karpenter 노드 반환 확인)
kubectl get nodes                                 # Karpenter 노드가 사라진 뒤
eksctl delete cluster --name skm-eks-cluster --region ap-northeast-2
eksctl delete cluster --name o11y-cluster --region ap-northeast-1
```

<details class="diagram-note">
<summary>이 블록이 하는 일</summary>

정리 순서다. 클러스터를 지우기 전에 k8s 리소스를 먼저 지워 Karpenter 가 만든 노드를 반환시킨다 — 노드가 남은 채 클러스터를 지우면 EC2 가 고아로 남아 과금이 계속된다. 두 리전에 클러스터가 하나씩이므로 각각 지운다.

</details>

```powershell
# 본 PC
cd skills-2026\set-07\task-2\module-3-eks-scaling\terraform ; terraform destroy -auto-approve
cd ..\..\module-4\terraform ; terraform destroy -auto-approve   # module-4를 배포했을 때만
```

- destroy 후 EC2/ALB/NAT가 콘솔에 안 남았는지 눈으로 확인한다.

:::caution[삭제 순서]
Karpenter 노드가 남은 채 클러스터를 지우면 **고아 EC2가 남는다** — NodePool 삭제 후 노드 반환을 확인하고 클러스터를 삭제한다.
:::
