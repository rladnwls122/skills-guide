---
title: Lab 15 — SQS · KEDA · Karpenter 오토스케일링
description: 유형 3 SQS→KEDA→Karpenter 스택을 배포하고, set-08 판으로 이행하며 유휴 파드 0 이 무엇을 무효로 만드는지 판정하는 실습.
sidebar:
  order: 3
---

import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';

> 문서 유형: tutorial

정답지: `skills-2026/set-07/task-2/` — 저장소가 아직 없다면 [사전 준비 §4 저장소 클론](/start/#4-저장소-클론)을 먼저 한다. 경로는 클론 루트 `~\skills-2026` 기준이다.

**손으로 쓰는 것과 가져다 쓰는 것을 먼저 갈라 둔다.**

| | 이 랩에서 |
|---|---|
| 그대로 쓴다 | 정답지의 terraform, `k8s/` 매니페스트(namespace·NodePool·ScaledObject), helm 차트 values, 제공 `app.py`·`worker.py` |
| 직접 쓴다 | Part B 의 `app/Dockerfile` 한 장 (제공 Dockerfile 은 flask 가 없어 CrashLoop 이다) |
| 값만 바꾼다 | 매니페스트의 `<ECR_IMAGE>`·`<SQS_URL>` 자리, helm 설치 옵션의 버전·리소스 값 |
 terraform은 본 PC(PowerShell 또는 리눅스 셸), 클러스터/헬름/이미지 빌드는 CloudShell(Docker 내장)에서 한다 — 채점과 같은 셸이라 클러스터 생성자 신원 = 채점 신원이 자동 일치한다. 상세 명령 전문은 각 모듈 README 런북에 있다. 이 랩은 순서·검증·함정에 집중한다.

## 실습 목표

- [ ] (유형 3) SQS→KEDA→Karpenter 스택을 배포하고 메시지 100건 → Pod ≥ 5 / Node ≥ 2, purge → Pod 1 / Node 1 수렴을 재현한다
- [ ] (유형 4) OTel→Loki→Grafana 파이프라인을 배포하고 LogQL로 ERROR 로그를 조회, 대시보드 3패널을 채운다
- [ ] (유형 5) 정답지 코드를 mark.md 2-1~2-6·4-1~4-5-B와 전부 대조한다 — 실제 배포는 선택(Part C)
- [ ] (이행) 유휴 파드를 1에서 0으로 내리고, **그 한 값이 무효로 만드는 필드와 필수로 만드는 필드**를 판정한다
- [ ] (이행) 트리거 인증을 별도 리소스로 옮긴다 — 도구 세대가 바뀌는 요구다
- [ ] 이행 파트에서 리전을 다시 세운다. **매니페스트 안에 박힌 리전 문자열**까지 센다
- [ ] 큐 기반 스케일링 진단에서 "큐에 남은 것"과 "소비자 상태"를 나눠 묻는다
- [ ] 클러스터와 VPC를 전부 정리한다

선행 지식과 목표는 [모듈 개요](/part-4/15-eks-scaling/)에.

:::note[실전 참조 — 막히면 여는 곳]
- CRD YAML 필드: `kubectl explain scaledobject.spec.triggers` · `kubectl explain nodepool.spec.disruption` (정본은 [KEDA Scalers](https://keda.sh/docs/latest/scalers/) · [Karpenter NodePool](https://karpenter.sh/docs/concepts/nodepools/))
- helm values 키: `helm show values kedacore/keda --version 2.20.1` · `helm show values oci://public.ecr.aws/karpenter/karpenter --version 1.14.0`
- LogQL 문법: [Loki 쿼리 문서](https://grafana.com/docs/loki/latest/query/)
- Terraform 인자(유형 5): [aws_kinesis_stream](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/kinesis_stream) · [aws_msk_cluster](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/msk_cluster) · [aws_cloudformation_stack](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/cloudformation_stack)
- 찾는 법 자체가 막히면 [실전 문서 검색 가이드](/reference/search-guide/)
:::

---

## 0. 변동 짝과 리전

이 모듈은 유형 3 하나를 깊게 판다. **후보 세트 둘이 같은 스택을 다른 값으로 쓰기 때문에** 이행 훈련이 붙는다.

| 파트 | 유형 | 기준본 | 리전 | 이행 타깃 | 리전 |
|---|---|---|---|---|---|
| A | 오토스케일링 | set-07 모듈3 | `ap-northeast-2` | — | — |
| A-이행 | 오토스케일링 | 위 A 의 결과 | | **set-08 모듈4** | **`us-west-2`** |
| B | 로깅 파이프라인 | set-07 모듈4 | `ap-northeast-1` | 짝 없음 | — |
| C | 스트리밍 | set-02 모듈2·4 | 개념 위주 | 짝 없음 | — |

:::danger[리전을 파트마다 다시 세운다]
기준본과 이행 타깃의 리전이 다르다. 리전을 틀리면 리소스는 만들어지고 `apply` 도 성공하지만 **채점이 지정 리전만 조회해 그 모듈이 0점이 된다.** 도구가 알려 주지 않는 유일한 종류다.

이행으로 넘어갈 때 `AWS_DEFAULT_REGION` 부터 다시 세운다. KEDA 트리거처럼 **매니페스트 안에도 리전 문자열이 박히는 자리**가 있으니 함께 센다.
:::

---

## Part A — 유형 3 · 오토스케일링 (기준본, ap-northeast-2, skm-)

### A-1. 절차 (순서 요약)

<Steps>

1. **본 PC**: `module-3-eks-scaling/terraform` apply → `outputs.json` → eksctl/k8s/mark/provided+outputs.json을 zip으로 CloudShell 에 올린다.
2. **CloudShell**: eksctl·helm 설치 → `~/.skm-env` 환경변수 영구화 → `eksctl create cluster`(~20분, **탭 닫지 말 것**). 클러스터가 2개(모듈 3·4)가 되면 **모듈 전용 터미널에서 `KUBECONFIG`를 모듈 경로로 고정**하고 시작한다 — 터미널 1개 = 클러스터 1개. 공유 kubeconfig의 `use-context` 전환은 잊는 순간 엉뚱한 클러스터에 apply 된다.
3. 앱 이미지 빌드/푸시 (제공 Dockerfile 그대로).
4. **KEDA 2.20.1** 설치 — `serviceAccount.operator.create=false`(IRSA용 keda-operator 재사용) + addon taint `tolerations` 지정.
5. **Karpenter 1.14.0** 설치(kube-system) — `--set replicas=1`(차트 기본 2는 addon 노드 1대에서 `helm --wait` 무한 대기) + `controller.resources.requests.cpu=0.5, memory=512Mi`로 낮춤(addon 노드 용량).
6. k8s 번호 순서 apply: namespace → **NodePool 먼저** → deployment → ScaledObject. deployment 는 자리표시자 두 개를 치환해 넘긴다.

   ```bash
   sed -e "s|<ECR_IMAGE>|$ECR_IMAGE|" -e "s|<SQS_URL>|$SQS_URL|" k8s/20-deployment.yaml | kubectl apply -f -
   ```

   매니페스트 파일 자체는 고치지 않는다 — 값만 갈아 끼워 apply 하면 다음 회차에 원본을 그대로 다시 쓴다.

</Steps>

### A-2. 검증 + 기대 출력

```bash
kubectl get pods -n keda                                      # 3개 모두 Running (Pending이면 A-4 함정 1)
kubectl get pods -n kube-system -l app.kubernetes.io/name=karpenter   # 1개 Running
kubectl get nodes -l karpenter.sh/nodepool=skm-app-nodepool   # 1대
kubectl get pods -n skillsmkt -o wide                         # order-processor 1/1 Running
kubectl get scaledobject -n skillsmkt                         # order-scaler READY True

# 스케일아웃: 메시지 100건 주입 (mark3.sh가 수행) 후 관찰
kubectl get pods -n skillsmkt -w        # 1 → 5 (queueLength 5, max 5)
kubectl get nodes -l karpenter.sh/nodepool=skm-app-nodepool   # 2대 이상 (500m×5 > 1.9 vCPU)

# 스케일인: purge 후 최대 2.5분 관찰
kubectl get pods -n skillsmkt           # 1개로 수렴
kubectl get nodes -l karpenter.sh/nodepool=skm-app-nodepool   # 1대로 수렴
```

<details class="diagram-note">
<summary>이 블록이 하는 일</summary>

스케일 동작을 위에서 아래로 확인한다. 먼저 KEDA·Karpenter·워커·ScaledObject 가 정상인지 본다. 그다음 메시지 100건을 넣고 파드가 1에서 5로, 노드가 따라 느는지 `-w` 로 지켜본다. 파드 5개의 요청량이 노드 한 대의 할당 가능 CPU 를 넘기 때문에 노드가 2대 이상이 되는 것이 정상이다. 큐를 비우면 반대로 수렴한다.

</details>

검증은 `bash ~/module-3/mark/mark3.sh` 다. 3-6 은 Max Ready Pods 5 와 Max App Nodes 2 이상, 3-7 은 Final Pods 1 과 Final Nodes 1 을 본다.

### A-3. :badge[함정]{variant=danger}

1. **KEDA Pod Pending** → 차트 tolerations 스키마 확인: `helm show values kedacore/keda --version 2.20.1 | grep -n -A3 tolerations`. 버전에 따라 키가 최상위/컴포넌트별로 바뀐다.
2. **addon 노드 용량 초과** → Karpenter requests 0.5/512Mi 축소 설치. 그래도 Pending이면 KEDA metricServer/webhooks requests도 축소.
3. **scaleDown stabilizationWindowSeconds(15)·consolidateAfter(60s)를 늘리면** 스케일인 채점(150초 창) 실패. ScaledObject에 pollingInterval은 두지 않는다 — min ≥ 1이면 스케일링이 HPA sync 주기를 따라 이 값이 감지 속도를 바꾸지 못한다.
4. **deployment env는 리터럴 3개만** — valueFrom/추가 금지 (name=value 정확 비교).
5. NodePool보다 deployment를 먼저 apply하면 앱 Pod가 스케줄될 노드가 없다 — 번호 순서 준수.
6. 디스커버리 태그 누락(서브넷 `karpenter.sh/discovery`, SG `aws:eks:cluster-name`) → 노드가 영원히 안 뜬다.
7. **Karpenter `helm --wait`가 안 끝나면 replicas부터** — 기본 2 + required podAntiAffinity라 addon 노드 1대에선 구조적으로 불가(`--set replicas=1`). 행 상태에서 중단했다면 release가 `pending-upgrade`로 잠긴다 → `helm uninstall karpenter -n kube-system` 후 재설치.

### A-4. 통과 기준

- [ ] order-scaler READY True, 초기 Pod 1 / 앱 노드 1
- [ ] 100건 주입: Pod ≥ 5, 앱 노드 ≥ 2
- [ ] purge: 2.5분 내 Pod 1 / 노드 1
- [ ] mark3.sh 전 항목 PASS

---

## Part A-이행 — set-08 SQS 스케일링 (us-west-2)

같은 스택(SQS + KEDA + Karpenter)을 다른 세트의 요구로 옮긴다. Part A 를 마친 상태에서 시작한다.

**파일 구조는 양쪽이 같다.** 바뀌는 것은 값이고, **그중 한 값이 다른 필드의 유효성을 뒤집는다.** 이 모듈의 판정 축이 그것이다.

### A이-1. 변동 과제지

**정답 코드를 먼저 열지 않는다.**

| 항목 | set-07 (기준본) | set-08 (변동) |
|---|---|---|
| 리전 | `ap-northeast-2` | **`us-west-2`** |
| 네임스페이스 | `skillsmkt` | `skills-sqs` |
| 큐 설정 | 기본값만 | **가시성 타임아웃을 명시** |
| 유휴 시 파드 수 | **1** | **0** |
| 최대 파드 수 | 5 | 6 |
| 파드 1개당 목표 메시지 | 5 | 2 |
| 트리거 인증 | 스칼라 옵션으로 지정 | **별도 리소스로 분리** |

#### 요구에서 파생되는 것

**"유휴 시 파드 0"** 이 이 이행의 전부다. 나머지는 숫자 교체지만 이 한 줄은 **다른 필드들의 필요성을 뒤집는다.**

파드가 최소 1개면 스케일링 판단을 쿠버네티스의 기본 오토스케일러가 자기 주기로 한다. 그래서 KEDA 쪽 폴링 주기를 적어도 **무효 필드**다 — 웹훅이 경고를 낸다.

파드가 0개가 되면 상황이 다르다. **0에서 1로 깨우는 일은 기본 오토스케일러가 못 한다.** 파드가 없으니 볼 지표도 없다. KEDA 가 직접 큐를 들여다보고 깨워야 하므로 **폴링 주기가 필수가 된다.**

축소 쪽도 같이 뒤집힌다. 최소 1개일 때는 기본 오토스케일러의 축소 동작을 조율해야 하지만, 0까지 내려가는 경우에는 KEDA 자신의 대기 시간이 그 자리를 맡는다.

**"트리거 인증을 별도 리소스로"** 는 도구 세대 문제다. 스칼라 옵션으로 지정하는 방식이 다음 주 버전에서 제거될 예정이라 별도 리소스로 옮겨간다. [08 의 프로바이더 버전 판정](/part-1/08-container-lambda-dynamodb/lab/)과 같은 종류다 — **요구가 코드가 아니라 도구 세대를 지정하는 경우**다.

### A이-2. 이행 (타이머 20분)

타이머를 걸고 시작한다. 아래는 절차가 아니라 **스스로 답해야 할 질문 목록**이다.

- 유휴 시 파드를 0으로 두면 **새로 필요해지는 필드**는 무엇이고, **무효가 되는 필드**는 무엇인가
- 그 판단의 근거를 어디서 확인하나. KEDA 문서인가 웹훅 경고인가
- 트리거 인증을 별도 리소스로 옮기면 리소스 종류 이름은 무엇이고, 트리거 쪽에서 그것을 어떻게 가리키나
- 리전 문자열이 박히는 자리가 몇 군데인가. Terraform 변수 말고 **매니페스트 안**에도 있나
- 네임스페이스를 바꾸면 따라오는 파일이 몇 개인가

**첫 질문에 시간을 쓴다.** 나머지는 문자열 교체다.

### A이-3. 통과 기준

<Steps>

1. **옛 이름·옛 리전 잔존 0건**

   <Tabs syncKey="shell">
     <TabItem label="PowerShell">
     ```powershell
     Select-String -Pattern 'skillsmkt|ap-northeast-2' -Path .\k8s\*,.\terraform\*
     ```
     </TabItem>
     <TabItem label="Bash (Linux/CloudShell)">
     ```bash
     grep -rnE 'skillsmkt|ap-northeast-2' k8s/ terraform/
     ```
     </TabItem>
   </Tabs>

   기대: **출력 없음.**

2. **적용 전에 웹훅에게 물어본다**

   ```bash
   kubectl apply --dry-run=server -f k8s/30-keda-scaledobject.yaml
   ```

   기대: `created (server dry run)`. **KEDA 웹훅이 무효 필드를 여기서 경고한다** — 유휴 파드 수와 폴링 주기의 조합이 어긋나면 이 단계에서 드러난다.

   경고가 나오면 코드가 아니라 **조합**이 틀린 것이다. A이-1 의 파생 설명으로 돌아간다.

3. **트리거 인증 리소스가 실제로 잡히나**

   ```bash
   kubectl apply --dry-run=server -f k8s/30-keda-scaledobject.yaml
   kubectl get crd | grep -i triggerauthentication
   ```

   기대: CRD 가 목록에 있다. 없으면 KEDA 버전이 그 리소스를 모르는 것이다.

</Steps>

#### set-08 채점 대응

[set-08 `mark/mark2-4.sh`](https://github.com/ishs-cloud-computing/skills-2026/blob/main/set-08/task-2/mark/mark2-4.sh) 가 이 파트를 잰다. 채점이 큐 ARN 과 가시성 타임아웃을 직접 뽑고, 메시지를 보낸 뒤 파드 수가 목표대로 늘어나는지 본다.

**파드 1개당 목표 메시지가 2 라는 것이 채점과 맞물린다.** 메시지 12건을 보내면 파드가 6개가 되어야 한다 — 최대치와 정확히 같다. 목표 값을 잘못 넣으면 파드 수가 어긋나 바로 드러난다.

### A이-4. 복기

[set-08 `module-4-sqs-scaling`](https://github.com/ishs-cloud-computing/skills-2026/tree/main/set-08/task-2/module-4-sqs-scaling)과 대조한다. **A이-3 을 통과한 뒤에 연다.**

특히 `k8s/30-keda-scaledobject.yaml` 의 **주석**을 읽는다. 정답지가 왜 그 필드를 넣고 뺐는지 이유를 적어 두었다 — 판단의 근거가 코드가 아니라 주석에 있는 드문 경우다.

---

## 6. 과제지 오독 복구

배포까지 했는데 값이 다르다. 이 모듈의 판정 축은 넷이다.

| 판정 | 무엇으로 아나 | 비용 |
|---|---|---|
| 즉시 수정 | 매니페스트를 고쳐 다시 적용하면 된다 | 초 |
| **조합이 틀렸다** | 값 하나하나는 맞는데 **웹훅이 경고**한다 | 초 — 다만 안 먹는다 |
| 도구 세대 | 필드를 아예 모른다 | 분 — 코드를 고치면 안 된다 |
| 리전 오독 | 아무 도구도 안 잡아 준다 | 전면 재배포 |

두 번째가 이 모듈에서 처음 나온다.

### 6-1. 파드당 목표 메시지를 기준본대로 뒀다

**상황.** 유휴 파드 수와 최대치는 맞췄는데 파드당 목표 메시지를 5 로 뒀다. 메시지 12건을 보내도 파드가 3개까지만 는다.

**판정.** 값 하나만 고쳐 다시 적용한다.

```bash
kubectl apply -f k8s/30-keda-scaledobject.yaml
kubectl get scaledobject -n skills-sqs
```

기대: 경고 없이 적용되고 곧 파드 수가 바뀐다. **오토스케일러 오브젝트는 값 교체가 자유롭다.**

**교훈.** 가장 싼 축이다. 다만 **채점이 파드 수를 직접 세므로** 값이 틀리면 바로 드러난다. 목표 값과 최대치와 보낼 메시지 수를 곱셈으로 맞춰 보고 시작한다.

### 6-2. 유휴 파드를 1로 둔 채 폴링 주기만 넣었다

**상황.** 새 요구를 읽고 폴링 주기를 넣었는데 유휴 파드 수는 기준본의 1 로 남겨 뒀다. 적용은 된다.

**판정.** 적용해 보면 안다.

```bash
kubectl apply --dry-run=server -f k8s/30-keda-scaledobject.yaml
```

기대: **경고가 나온다.** 유휴 파드가 1 이상이면 스케일링 판단을 기본 오토스케일러가 하므로 KEDA 쪽 폴링 주기가 **무효 필드**다. 적용은 되는데 그 값은 아무 일도 하지 않는다.

**여기가 이 모듈의 핵심이다.** 값 하나하나는 전부 유효한데 **조합이 틀렸다.** 문법 오류도 아니고 오타도 아니라서, 값만 훑어서는 못 찾는다.

**복구.** 유휴 파드를 0 으로 내린다. 그러면 폴링 주기가 필수가 되고 경고가 사라진다.

**파급.** 0 으로 내리면 축소 쪽 설정도 함께 봐야 한다. 기준본이 기본 오토스케일러의 축소 동작을 조율하고 있었다면 그 블록은 더 이상 맞지 않는다.

**교훈.** **한 값이 다른 필드의 유효성을 정하는 경우가 있다.** 요구를 읽을 때 "이 값이 바뀌면 무엇이 함께 의미를 잃나"를 묻는다. 그리고 **웹훅 경고를 무시하지 않는다** — 적용이 됐다고 먹은 것은 아니다.

### 6-3. 트리거 인증을 기준본 방식으로 뒀다

**상황.** 스칼라 옵션으로 인증 주체를 지정하는 기준본 방식을 그대로 뒀다. KEDA 버전에 따라 동작하기도 하고 안 하기도 한다.

**판정.** 클러스터의 KEDA 가 그 방식을 아직 지원하는지 본다.

<Tabs syncKey="shell">
  <TabItem label="PowerShell">
  ```powershell
  kubectl get crd | Select-String -Pattern 'keda'
  kubectl -n keda get deploy -o jsonpath='{.items[0].spec.template.spec.containers[0].image}'
  ```
  </TabItem>
  <TabItem label="Bash (Linux/CloudShell)">
  ```bash
  kubectl get crd | grep keda
  kubectl -n keda get deploy -o jsonpath='{.items[0].spec.template.spec.containers[0].image}'
  ```
  </TabItem>
</Tabs>

기대: CRD 목록에 트리거 인증 리소스가 있고, 이미지 태그로 버전을 확인할 수 있다.

**복구.** 인증을 별도 리소스로 분리하고 트리거에서 그것을 가리킨다. 코드 구조가 바뀌는 것이지 값 교체가 아니다.

**교훈.** [08 의 프로바이더 버전](/part-1/08-container-lambda-dynamodb/lab/)과 같은 자리다. **과제지가 도구 세대를 지정하는 경우**가 있고, 그때는 코드를 고치는 것이 아니라 방식을 옮긴다. 판정 순서는 ① 오타인가 ② 도구가 아는가 ③ 그다음 문서다.

## 7. 장애 진단

정상으로 선 상태에서 일부러 고장을 내고 증상만 보고 좁혀 간다. **주입 명령과 복구 명령이 짝으로 있다.**

### 7-1. 메시지를 넣어도 파드가 안 는다

**주입.** KEDA 가 큐를 못 보게 한다. 운영자 서비스 계정의 신원 연결을 끊는다.

<Tabs syncKey="shell">
  <TabItem label="PowerShell">
  ```powershell
  kubectl -n keda get sa keda-operator -o yaml | Select-String -Pattern 'role-arn'
  kubectl -n keda annotate sa keda-operator eks.amazonaws.com/role-arn- --overwrite
  kubectl -n keda rollout restart deploy keda-operator
  ```
  </TabItem>
  <TabItem label="Bash (Linux/CloudShell)">
  ```bash
  kubectl -n keda get sa keda-operator -o yaml | grep role-arn
  kubectl -n keda annotate sa keda-operator eks.amazonaws.com/role-arn- --overwrite
  kubectl -n keda rollout restart deploy keda-operator
  ```
  </TabItem>
</Tabs>

첫 명령의 출력을 적어 둔다 — 복구할 때 그 값을 되돌린다.

**증상.** 큐에 메시지가 쌓이는데 파드가 그대로다. 오토스케일러 오브젝트는 존재하고 앱도 정상이다.

**좁히는 순서.** 판단하는 쪽에서 대상 쪽으로 간다.

<Steps>

1. **오토스케일러 오브젝트가 준비됐나** — `kubectl get scaledobject -n <ns>`. `READY` 가 `False` 면 트리거를 못 읽는 것이다. **이 한 열이 갈림이다**

2. **왜 준비가 안 됐다고 하나** — `kubectl describe scaledobject -n <ns> <이름>`. 조건에 이유가 글자로 적혀 있다

3. **판단하는 쪽 로그** — `kubectl logs -n keda deploy/keda-operator --tail=50`. 큐 조회 실패면 여기에 찍힌다. `AccessDenied` 계열이면 신원 문제다

4. **신원이 붙어 있나** — 운영자 서비스 계정의 역할 연결을 본다. 없으면 여기가 원인이다

5. 신원이 있는데도 막히면 그때 역할의 정책과 **큐가 다른 리전에 있는지**를 본다

</Steps>

마지막 줄이 이 모듈 특유다. **트리거에 적은 리전이 큐의 리전과 다르면 조회가 조용히 빈다** — 권한 문제처럼 보이지만 아니다.

**복구.** 적어 둔 역할을 다시 붙인다.

<Tabs syncKey="shell">
  <TabItem label="PowerShell">
  ```powershell
  kubectl -n keda annotate sa keda-operator eks.amazonaws.com/role-arn=<적어-둔-ARN> --overwrite
  kubectl -n keda rollout restart deploy keda-operator
  kubectl get scaledobject -n skills-sqs
  ```
  </TabItem>
  <TabItem label="Bash (Linux/CloudShell)">
  ```bash
  kubectl -n keda annotate sa keda-operator eks.amazonaws.com/role-arn=<적어-둔-ARN> --overwrite
  kubectl -n keda rollout restart deploy keda-operator
  kubectl get scaledobject -n skills-sqs
  ```
  </TabItem>
</Tabs>

기대: `READY` 가 `True` 로 돌아오고 곧 파드가 늘어난다.

### 7-2. 큐를 비웠는데 파드가 안 줄어든다

**주입.** 메시지를 소비하지 못하게 막는다. 워커의 이미지를 없는 태그로 바꿔 새 파드가 못 뜨게 한다.

<Tabs syncKey="shell">
  <TabItem label="PowerShell">
  ```powershell
  kubectl set image deployment/sqs-worker -n skills-sqs sqs-worker=nosuchimage:v0 
  ```
  </TabItem>
  <TabItem label="Bash (Linux/CloudShell)">
  ```bash
  kubectl set image deployment/sqs-worker -n skills-sqs sqs-worker=nosuchimage:v0
  ```
  </TabItem>
</Tabs>

컨테이너 이름은 `kubectl get deploy sqs-worker -n skills-sqs -o jsonpath='{.spec.template.spec.containers[0].name}'` 로 확인한다.

**증상.** 큐를 비웠는데 파드 수가 오래 그대로다. 또는 메시지를 넣었는데 처리가 안 되고 계속 쌓인다.

**좁히는 순서.**

<Steps>

1. **큐가 정말 비었나** — 가시성 타임아웃 때문에 **처리 중인 메시지는 목록에서 안 보인다.** `ApproximateNumberOfMessagesNotVisible` 까지 함께 본다. 이것을 모르면 "비웠는데 왜"에서 막힌다

2. **파드가 뜨긴 하나** — `kubectl get pod -n <ns>`. `ImagePullBackOff` 면 소비자가 없어 메시지가 안 줄어드는 것이다

3. **축소가 대기 중인가** — 축소에는 대기 시간이 있다. 방금 비웠으면 그 시간만큼 기다린다

4. **노드까지 줄어드나** — 파드가 줄어도 노드는 별도 판단이다. 노드 정리 대기 시간이 따로 있다

</Steps>

**1번이 이 모듈 고유의 함정이다.** 큐의 "보이는 메시지"만 보면 비어 있는데, 처리 중인 것이 남아 있으면 오토스케일러는 아직 일이 있다고 본다. **가시성 타임아웃을 명시하라는 요구가 여기와 이어진다** — 값이 길면 실패한 처리가 오래 묶여 있다.

**복구.**

<Tabs syncKey="shell">
  <TabItem label="PowerShell">
  ```powershell
  kubectl rollout undo deployment/sqs-worker -n skills-sqs
  kubectl rollout status deployment/sqs-worker -n skills-sqs
  ```
  </TabItem>
  <TabItem label="Bash (Linux/CloudShell)">
  ```bash
  kubectl rollout undo deployment/sqs-worker -n skills-sqs
  kubectl rollout status deployment/sqs-worker -n skills-sqs
  ```
  </TabItem>
</Tabs>

**교훈.** 큐 기반 스케일링의 진단은 **"큐에 무엇이 남아 있나"와 "소비자가 살아 있나"를 나눠 묻는 것**에서 시작한다. 둘을 합쳐 보면 원인을 못 짚는다.

## 8. 정리 (당일 필수 — EKS 가 최대 과금원)

```bash
# CloudShell (ap-northeast-2)
kubectl delete -f k8s/ --ignore-not-found        # ScaledObject·NodePool 먼저 (Karpenter 노드 반환 확인)
kubectl get nodes                                 # Karpenter 노드가 사라진 뒤
eksctl delete cluster --name skm-eks-cluster --region ap-northeast-2
```

<details class="diagram-note">
<summary>이 블록이 하는 일</summary>

정리 순서다. 클러스터를 지우기 전에 k8s 리소스를 먼저 지워 Karpenter 가 만든 노드를 반환시킨다 — 노드가 남은 채 클러스터를 지우면 EC2 가 고아로 남아 과금이 계속된다.

</details>

<Tabs syncKey="shell">
  <TabItem label="PowerShell">
  ```powershell
  # 본 PC
  cd ~skills-2026set-07	ask-2module-3-eks-scaling	erraform ; terraform destroy -auto-approve
  ```
  </TabItem>
  <TabItem label="Bash (Linux/CloudShell)">
  ```bash
  # 본 PC
  cd ~/skills-2026/set-07/task-2/module-3-eks-scaling/terraform && terraform destroy -auto-approve
  ```
  </TabItem>
</Tabs>

이행 파트(us-west-2)를 실제로 배포했다면 그쪽 클러스터도 같은 순서로 지운다.

- destroy 후 EC2/ALB/NAT가 콘솔에 안 남았는지 눈으로 확인한다.

:::caution[삭제 순서]
Karpenter 노드가 남은 채 클러스터를 지우면 **고아 EC2가 남는다** — NodePool 삭제 후 노드 반환을 확인하고 클러스터를 삭제한다.
:::

## 다음

로깅 파이프라인은 [16. 컨테이너 로깅](/part-4/16-container-logging/), 스트리밍은 [17. 스트리밍](/part-4/17-streaming/)으로 갈라져 있다.
