---
title: 과제지·채점지 정정 내역
description: 이의 제기로 확정된 과제지·채점지 정정 결과를 세트별로 모은 표. 배포된 파일과 실제 채점 기준이 갈리는 지점을 여기서 확인한다.
---

> 문서 유형: reference

배포된 과제지·채점지 파일과 **실제 채점 기준이 다를 수 있다.** 이의 제기로 확정된 정정은 별도 공지로 오고 파일이 항상 다시 배포되지는 않는다. 이 문서는 그 차이를 모은다 — 정답지나 이 사이트의 다른 문서가 정정 전 기준으로 쓰여 있으면 **여기가 우선**이다.

## 정정 절차와 일정 :badge[협의회]{variant=note}

지도교사 협의회(2026-07-31)에서 확정된 절차다.

- **오류 제기·수정 마감**: 대회 10일 전인 **8월 13일**. 접수된 건은 72시간(3일) 이내 처리된다.
- **정정 방식**: 과제지 파일 자체는 수정하지 않고, 내용 정정은 심사위원 측에서 진행한다. 아래 표에 "과제지에 명시"라고 적힌 항목도 배포본이 그대로일 수 있다 — 공지된 내용이 기준이다.
- 13일 이후에도 오류가 나올 수 있어 추가 정정은 불가피하다는 답변이 있었다. **대회 직전까지 공지를 다시 확인한다.**
- set-08 채점지는 정확한 확인 방법이 명시되지 않은 항목이 있어, 기존 파일을 고치는 대신 **예상 출력값을 별도 텍스트 파일로 제공할 예정**이다. 그 파일이 나오면 출력형 채점의 기대 출력 정본이 된다.

## set-02 · 1과제

| 항목 | 제기된 문제 | 확정된 정정 |
|---|---|---|
| 채점 7-2 | 예상 출력에 `/book*`·`wskorea26-cf`·`403` 3줄만 적혀 있으나, ALB 리스너 규칙이 2개(POST → EKS, GET → Lambda)라 실제로는 `/book*`·`wskorea26-cf`가 **각 2회** 출력된다 | 채점기준표의 예상 출력을 실제 출력(2회)에 맞게 수정 |
| 커스텀 헤더 | `wskorea26-s3-access` 헤더의 **값**이 과제지에 없음 | 과제지에 `wskorea26-s3-access: true`로 값 명시 |
| Pod 로그 | "모든 모니터링 도구는 Addon 노드그룹에서 동작"과 "Pod 로그는 CloudWatch Logs로 전송"이 충돌 — 후자는 DaemonSet(Fluent Bit)이나 사이드카를 요구한다 | **채점 대상이 아니다.** 초기 설계에서 Fluent Bit를 고려하며 넣었다가 최종 요구사항에서 빠지는 과정에 남은 잔여 문구로, 과제지에서 삭제된다 |
| 배점 | 표기상 소계가 총점과 불일치 | **3-1 · 10-1 · 10-2의 배점을 1.5 → 1로 변경** |
| 채점 10-1 | Grafana 비밀번호가 채점지에는 `\$korea26!!`, 채점 스크립트 출력에는 `$korea26!!`로 나와 표기가 어긋남 | 채점기준표 기준. 백슬래시는 셸 이스케이프 표기이고 **값은 `$korea26!!`** 다 |

## set-02 · 2과제

| 항목 | 제기된 문제 | 확정된 정정 |
|---|---|---|
| 모듈 1 | 처리용 Lambda 이름이 과제지에 없고 채점만 `wsc2026-student-score-function`을 요구 | **과제지에 정확한 이름을 명시** — 더 이상 "채점지에만 있는 이름"이 아니다 |
| 모듈 2 | 과제지는 "Notebook에서 SQL 쿼리가 정상 실행"(=RUNNING)을 요구하는데 채점은 `ApplicationStatus == Ready`를 본다 | **의도된 동작이다.** 시연 후 Stop해 READY로 되돌린다 |
| 모듈 2 | EC2 역할명 `wsc2026-alaytics-ec2-role`의 `alaytics` 오타 | **과제지의 오타를 수정한다. 채점에는 영향을 주지 않는다** — 이 이름은 채점 대상이 아니다 |
| 모듈 2 | systemd 서비스 이름 `app`이 과제지·제공 파일 어디에도 없음 | 과제지에 서비스 이름이 `app`임을 명시 |
| 전 모듈 | 직종설명서는 2과제 배포파일을 Python·CSV로 제한하는데 실제로는 Markdown이 배포됨 | **수정하지 않는다.** 심사위원이 직접 확인해 축소 가능한 내용이 있으면 축소 |
| 모듈 4 | 채점 4-1이 S3 버킷 ARN을 계정 번호까지 포함한 고정값과 "일치하는지" 보는데, 버킷 이름에는 선수마다 다른 비번호가 들어간다 | **비번호 부분을 제외하고 채점** |
| 모듈 4 | 채점 4-5-A·4-5-B의 "아래와 같은 형식으로 출력되는지" 기준이 모호 | **수정하지 않는다.** 값 일치가 아니라 **JSON 출력 형식**을 보는 항목이고, 확인 대상 키는 붉은색 강조로 식별된다 |
| 모듈 4 | 채점 4-5-B의 명령은 `--output table`인데 예시 결과는 JSON이고, `max-items 3`이라 3건이 출력됨 | 명령을 **`--output json`으로 수정**하고 `SENSOR-002`의 `sensorId`·`timestamp`만 조회하도록 변경. 출력에 두 항목만 포함되고 `timestamp`가 **ISO 8601 KST**(`2026-06-01T18:28:24+09:00`)면 정답 |

:::danger[이 정정이 뒤집는 기존 지침 — `alaytics`]
"과제지 오타는 그대로 쓴다"의 대표 사례였던 `wsc2026-alaytics-ec2-role`은 **정정으로 해소됐다.** 오타는 수정되고 이 이름은 채점 대상이 아니다. 원칙 자체(이름 정확 일치 채점에서는 적힌 그대로 쓴다)는 그대로 유효하지만, **정정 공지를 확인하지 않으면 이미 고쳐진 오타를 "사양"으로 착각한다.** 이름을 확정하기 전에 이 문서를 먼저 본다.
:::

## set-08 · 2과제 (제기됨 — 수정 예정)

아직 확정 공지가 아니라 **출제자가 고치겠다고 답한 단계**다. 배포본 그대로 연습하되 채점 방식이 바뀔 수 있다는 전제로 본다.

| 항목 | 제기된 문제 | 상태 |
|---|---|---|
| 전 모듈 | 채점지가 "요구사항이 충족하는지 확인합니다"로만 적혀 있어 **판정 기준이 모호하다.** 실제 채점은 채점지에 적힌 결과만 근거로 하므로, 명령 결과값을 채점지에 표시하고 정확 일치·부분 일치로 구분해 표기해야 한다 | 수정 예정 |
| 전 모듈 | **채점지의 명령과 채점 스크립트의 명령이 다르다.** 예로 4-1은 채점지가 `aws eks describe-cluster ... --output table`인데 스크립트는 `--query 'cluster.{Name:name,Status:status,...}'`로 특정 필드만 파싱한다 — 두 소스의 출력이 애초에 다르다 | 수정 예정 (출제자가 통일하겠다고 답변) |

:::caution[출력형 채점에서 두 소스가 갈리면 기대 출력도 갈린다]
출력형 스크립트는 **무엇을 출력하는가**가 곧 사양이다([mark.sh 읽는 법](/reference/mark-script-guide/)). 채점지와 스크립트의 명령이 다르면 어느 쪽 출력을 기준으로 채점될지 알 수 없다 — 통일되기 전까지는 **양쪽 명령을 모두 돌려 보고 둘 다 기대 형태로 나오는지** 확인한다. 특히 `--query`로 필드를 좁히는 쪽이 더 엄격하므로 그쪽을 기준으로 맞춰 두면 안전하다.
:::

## 반영 상태

이 사이트에서 위 정정이 반영된 곳이다. 정정 전 기준으로 남아 있는 서술을 발견하면 이 표부터 갱신한다.

| 정정 | 반영된 문서 |
|---|---|
| Pod 로그 CloudWatch 전송 요구 삭제 | [모듈 07 이론](/part-2/07-full-deploy-set02/theory/) — set-02 선정 시 Fluent Bit 불필요 |
| Grafana 비밀번호 표기 | [mark.sh 읽는 법](/reference/mark-script-guide/) |
| 배점 3-1·10-1·10-2 (1.5 → 1) | 이 문서에만 기록 — 사이트가 세부 배점을 싣지 않는다 |
| `wskorea26-s3-access: true` 값 명시 | [모듈 07 이론](/part-2/07-full-deploy-set02/theory/) — 이미 값까지 적혀 있어 변경 없음 |
| 처리 Lambda 이름이 과제지에 명시됨 | [모듈 09 이론](/part-4/09-serverless-event/theory/) · [mark.sh 읽는 법](/reference/mark-script-guide/) |
| `alaytics` 오타 해소 | [모듈 10 이론](/part-4/10-scaling-logging-streaming/theory/) · [모듈 15 이론](/part-6/15-mutation-drill/theory/) · [mark.sh 읽는 법](/reference/mark-script-guide/) |
| 4-5-B 출력 형식 | [모듈 10 이론](/part-4/10-scaling-logging-streaming/theory/) |
| set-08 채점지 ↔ 스크립트 명령 불일치 | [mark.sh 읽는 법](/reference/mark-script-guide/) |
